---
name: security-review
description: |
  Аудит безопасности изменений текущей ветки: утечки секретов, инъекции, mass assignment,
  SSRF, открытые редиректы, XSS, утечка данных в ответах, транспорт и CORS, PII в логах,
  риски новых зависимостей. Привязки под стек берутся автоматически.
disable-model-invocation: true
---

Проведи аудит безопасности изменений текущей ветки.

1. Определи базовую ветку и активные стеки проекта по скиллу `detect-stack`.
2. Запусти агента **agent-security-reviewer** через Agent tool, передав в промпте:
   - `BASE_BRANCH` и `BASE` (хеш merge-base);
   - имена картриджей стека;
   - строку: «Одиночный прогон: auth-reviewer не запускался — покрой добором ключевые пункты
     доступа (IDOR, незащищённые эндпоинты, хранение токенов, rate-limit на аутентификации)».
3. Верни полный отчёт агента пользователю как есть.
