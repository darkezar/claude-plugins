---
name: design-review
description: |
  Проверка соответствия дизайн-системе проекта: цвета и отступы из токенов вместо хардкода,
  переиспользование общих UI-примитивов, типографика, светлая и тёмная схемы, локализация без
  захардкоженных строк, форматирование денег и дат через общие хелперы.
disable-model-invocation: true
---

Проверь изменения текущей ветки на соответствие дизайн-системе проекта.

1. Определи базовую ветку и активные стеки проекта по скиллу `detect-stack`.
2. Запусти агента **agent-design-reviewer** через Agent tool, передав в промпте:
   - `BASE_BRANCH` и `BASE` (хеш merge-base);
   - имена картриджей стека;
   - строку: «Одиночный прогон: остальные ревьюеры не запускались».
3. Верни полный отчёт агента пользователю как есть.

Если в проекте нет вёрстки (чистый API-бэкенд), скажи об этом и не запускай агента.
